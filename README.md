
## Цей репозиторій містить рішення завдань із системного програмування під Linux мовою C (Практична робота №8). Робота присвячена поглибленому вивченню низькорівневих системних викликів для роботи з файлами (`write`, `read`, `lseek`), управління процесами (`fork`) та дослідження поведінки системних функцій (`qsort`).


## 📋 Опис завдань (Загальна частина)Task 8.1 (task8_1.c) — Дослідження write(): Демонструє явище "часткового запису" (partial write). За допомогою системного ліміту setrlimit програма штучно змушує write() записати менше байтів, ніж було запрошено, доводячи, що повернене значення може відрізнятися від розміру переданого буфера.Task 8.2 (task8_2.c) — Робота з lseek(): Показує механізм позиціонування у файлі. Програма створює бінарний файл, переміщує вказівник читання на 3-й байт за допомогою lseek(fd, 3, SEEK_SET) і читає наступні 4 байти, перевіряючи правильність зміщення (offset).Task 8.3 (task8_3.c) — Дослідження алгоритму qsort(): Програма тестує системну реалізацію Quick Sort на масивах різних типів (випадкові, відсортовані, зворотно відсортовані, з однаковими елементами, V-подібні), щоб виявити найгірші вхідні дані для алгоритму.Task 8.4 (task8_4.c) — Робота з fork(): Демонструє базовий механізм породження процесів. Доводить, що після успішного виклику fork() код виконують два незалежні процеси одночасно: дочірній (де fork повертає 0) та батьківський (де fork повертає PID нащадка).🚀 Індивідуальне завдання (Варіант 11)Task 8.11 (task8_var11.c) — Аналіз часу виконання qsort() та пояснення сплесків:Завдання: Побудувати таблицю з часами виконання qsort() для різних схем вхідних даних (5 млн елементів) і пояснити сплески в часі.Результати та АналізПрограма генерує масиви для 6 різних конфігурацій та вимірює час сортування у мілісекундах.Природа "сплесків" у часі виконання:Теоретична вразливість Quick Sort: У наївній реалізації, якщо опорний елемент (pivot) вибирається невдало (наприклад, масив уже відсортований, зворотно відсортований або має V-подібну форму), алгоритм не може ефективно розділити дані. Складність деградує від ідеальних $O(N \log N)$ до найгірших $O(N^2)$, що викликає гігантські сплески часу. Те ж саме стосується масивів з винятково однаковими елементами через велику кількість зайвих обмінів.Практична поведінка в Linux (glibc): Системна функція qsort є високооптимізованою (часто використовує Merge Sort під капотом при достатній пам'яті). Тому вона вміє уникати $O(N^2)$ для "поганих" масивів. На практиці найзначніший час (сплеск) найчастіше фіксується на масивах із випадковими числами або майже відсортованих (з 1% хаосу) масивах, оскільки процесору набагато важче застосовувати передбачення розгалужень (branch prediction) при роботі з хаотичними даними порівняно з лінійними патернами.
